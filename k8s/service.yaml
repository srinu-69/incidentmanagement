apiVersion: v1
kind: Service
metadata:
  name: incident-service
spec:
  type: NodePort
  selector:
    app: incident-app
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30007